apiVersion: v1
kind: Service
metadata:
  name: demo-walt-frontend
  namespace: {{ $.Release.Namespace | quote }}
spec:
  type: ClusterIP
  ports: 
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http-api
  selector:
    app.kubernetes.io/instance: demo-walt-frontend
    app.kubernetes.io/name: demo-walt-frontend